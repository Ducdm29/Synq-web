Album art, artist, or user avatar with an initials fallback.

```jsx
<Avatar src={cover} shape="rounded" size={56} />
<Avatar name="An Nhiên" ring />
```

`shape="circle"` for people, `"rounded"` for album/cover art. `ring` adds a mint accent ring (e.g. live/now-playing).
